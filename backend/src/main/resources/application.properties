server.port=8080

spring.application.name=techcontent-ai

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/techcontent}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

ml.service.url=${ML_SERVICE_URL:http://localhost:5000}
supabase.auth.url=${SUPABASE_AUTH_URL:http://localhost:9999}
supabase.anon.key=${SUPABASE_ANON_KEY:}
supabase.service.key=${SUPABASE_SERVICE_KEY:}
supabase.jwt.secret=${SUPABASE_JWT_SECRET:}

oci.files.bucket=${OCI_FILES_BUCKET:techcontent-files}
# Configuración por defecto para OCI (añadida para evitar errores en pruebas)
oci.objectstorage.namespace=${OCI_NAMESPACE:mock-namespace}
oci.objectstorage.bucket-name=${OCI_FILES_BUCKET:techcontent-files}
oci.region=${OCI_REGION:us-ashburn-1}
oci.tenant-id=${OCI_TENANT_ID:ocid1.tenant.oc1..mock}
oci.user-id=${OCI_USER_ID:ocid1.user.oc1..mock}
oci.fingerprint=${OCI_FINGERPRINT:00:00:00:00:00:00:00:00:00:00:00:00:00:00:00:00}
oci.private-key.path=${OCI_PRIVATE_KEY_PATH:mock-path}

spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC
