server.port=8080

spring.application.name=techcontent-ai

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/techcontent}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Actuator — solo health e info, detalles solo para usuarios autenticados
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when_authorized

# HTTP client timeouts para RestClient (MlClient, SupabaseAuthClient)
spring.http.client.connect-timeout=5s
spring.http.client.read-timeout=30s

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

ml.service.url=${ML_SERVICE_URL:http://localhost:5000}
supabase.auth.url=${SUPABASE_AUTH_URL:http://localhost:9999}
supabase.anon.key=${SUPABASE_ANON_KEY:}
supabase.service.key=${SUPABASE_SERVICE_KEY:}
supabase.jwt.secret=${SUPABASE_JWT_SECRET:}
supabase.jwks.url=${SUPABASE_JWKS_URL:https://bftvhnxtbahabwykqwni.supabase.co/auth/v1/.well-known/jwks.json}

# OCI Object Storage
oci.files.bucket=${OCI_FILES_BUCKET:techcontent-files}
oci.dataset.bucket=${OCI_DATASET_BUCKET:your-graphrag-dataset-bucket}
oci.namespace=${OCI_NAMESPACE:your-namespace}
oci.region=${OCI_CLI_REGION:us-phoenix-1}
oci.tenancy=${OCI_CLI_TENANCY:}
oci.user=${OCI_CLI_USER:}
oci.fingerprint=${OCI_CLI_FINGERPRINT:}
oci.private-key-path=${OCI_CLI_KEY_FILE:}

logging.level.com.techcontent.ai.integration=${INTEGRATION_LOG_LEVEL:INFO}
logging.level.com.techcontent.ai.integration.ml=${ML_LOG_LEVEL:INFO}
logging.level.com.techcontent.ai.integration.oci=${OCI_LOG_LEVEL:INFO}

# CORS — en produccion setear CORS_ALLOWED_ORIGINS al dominio del frontend
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:*}

spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC
